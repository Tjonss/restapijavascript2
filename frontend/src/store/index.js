import { createStore } from 'vuex'
import books from './modules/books'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    books
  }
})
